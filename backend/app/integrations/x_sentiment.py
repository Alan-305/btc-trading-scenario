"""X (Twitter) sentiment integration — Phase 5 placeholder."""

from app.schemas.market import SentimentIndicators


class XSentimentClient:
    async def fetch_score(self) -> float | None:
        return None
